# Copy to backend/.env and fill in real values.
# Loaded automatically by python-dotenv at startup.

# Required: your OpenAI API key (used for embeddings + gpt-4o chat).
OPENAI_API_KEY=sk-your-openai-key-here
